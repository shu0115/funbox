class GoodsController < ApplicationController
  # def index
  #   @playlists = Playlist.includes(:goods).where(id: Good.mine(current_user).pluck(:playlist_id)).order("goods.created_at DESC")
  # end
end
